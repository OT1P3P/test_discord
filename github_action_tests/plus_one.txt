(define plus_one (lambda (n)
    (+ n 1)))

(display "Résultat pour (plus_one 5) : ")
(display (plus_one 5)) ;

(display "Résultat pour (plus_one 10) : ")
(display (plus_one 10)) ;

(display "Résultat pour (plus_one -3) : ")
(display (plus_one -3)) ;

